/** Initializes a new Jack Be NIMble game and starts running it. */
class Main {
    function void main() {
        var NIMGame game;
        var Array heapSizes;

        let heapSizes = Array.new(3);
        let heapSizes[0] = 1;
        let heapSizes[1] = 2;
        let heapSizes[2] = 3;

        let game = NIMGame.new(3, heapSizes, 0);

        //do game.play();
        do game.describe();
        do game.dispose();
        return;
    }
}